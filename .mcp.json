{
  "mcpServers": {
    "forge": {
      "args": [
        "-y",
        "automagik-forge",
        "--mcp"
      ],
      "command": "npx"
    },
    "omni": {
      "command": "uvx",
      "args": ["automagik-tools", "tool", "omni"],
      "env": {
        "OMNI_API_KEY": "namastex888",
        "OMNI_BASE_URL": "http://localhost:8882"
      }
    }
  }
}
