# Omni API Configuration
VITE_API_URL=http://localhost:8882
VITE_API_KEY=your-secret-api-key-here

# UI Server Configuration (Optional - will use defaults if not set)
UI_HOST=0.0.0.0
UI_PORT=9882

# NOTE: Evolution API authentication is now fully automatic using per-instance keys.
# The UI automatically retrieves instance-specific tokens from the health endpoint.
# No Evolution API configuration is needed in this file.
