# Omni API Configuration
VITE_API_URL=http://localhost:8882
VITE_API_KEY=your-secret-api-key-here

# Evolution API Key (temporary - will be replaced with per-instance keys)
# This is the bootstrap key used for Evolution API authentication
# Copy the value from root .env EVOLUTION_API_KEY
VITE_EVOLUTION_API_KEY=your-evolution-api-key-here

# UI Server Configuration (Optional - will use defaults if not set)
UI_HOST=0.0.0.0
UI_PORT=9882
